/**
 * SdlWindow Constants
 */

/** Milliseconds per second, for converting Hz to interval */
export const MS_PER_SECOND = 1_000;

/** Default event loop interval in ms (~60 fps) when refresh rate detection fails */
export const DEFAULT_EVENT_LOOP_INTERVAL_MS = 16;

/** How often to check for refresh rate changes (in milliseconds) */
export const REFRESH_RATE_CHECK_INTERVAL_MS = 1_000;
